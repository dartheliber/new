DROP INDEX ISMA.EMP_FUNCION;    --eliminar indices

ALTER TABLE ISMA.OPE_EMP_FUNCION ADD (
  CONSTRAINT PK_EMP_FUNCION
  PRIMARY KEY
  (FCMANDT, FCFUNCION)
  USING INDEX EMP_FUNCION);

ALTER TABLE ISMA.OPE_EMP_FUNCION ADD   --agregar constraint de primary key
CONSTRAINT PK_EMP_FUNCION 
PRIMARY KEY 
(FCMANDT, FCFUNCION,FCLENGUAJE);

SELECT *
FROM OPE_EMP_FUNCION;

TRUNCATE TABLE OPE_EMP_FUNCION;

SELECT *                                                        --ver todos los indices de una tabla
FROM ALL_INDEXES
WHERE TABLE_NAME = 'OPE_EMP_FUNCION';

ALTER INDEX ISMA.PK_EMP_FUNCION REBUILD;